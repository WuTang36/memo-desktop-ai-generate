# 备忘录桌面应用 — 对话总结

> 日期：2026-08-13 ~ 2026-08-15

---

## 1. 项目启动：Web 应用 → Electron 桌面客户端

将原有的纯 HTML/CSS/JS 备忘录应用改造为 **Electron + React + TypeScript** 桌面应用。

**技术栈**：`electron-vite` · `React 18` · `TypeScript 5` · `Vitest 2` · `CodeMirror 6`

**项目结构**：`memo-app-desktop/`，包含 main 进程、preload 脚本、renderer（React 组件 + hooks + store）。

---

## 2. 环境搭建与踩坑

| 问题 | 解决方案 |
|------|----------|
| npm install 很慢 | 建议配置国内镜像源 (`npmmirror.com`) |
| macOS 阻止 Electron 运行，移到废纸篓 | `xattr -cr` 去除隔离属性 |
| macOS 报"包含恶意软件" | `sudo spctl --master-disable` 允许任何来源 |
| run dev 后控制台报错 | 定位并修复了渲染进程中的错误 |

---

## 3. Markdown 支持

为备忘录新增了 Markdown 编辑与预览功能：

- **编辑模式**：CodeMirror 编辑器 + Markdown 语法高亮
- **预览模式**：`react-markdown` 渲染 Markdown 为富文本
- 右上角提供两个模式切换按钮

---

## 4. 测试与修复

- 测试框架：**Vitest + @testing-library/react**
- 共 **59 个测试用例**，分布在 5 个测试文件中，**100% 通过**
- 修复了 3 个问题：
  1. 取消 Enter 键自动保存（改为仅 Ctrl+Enter 保存）
  2. 预览模式下不再显示"写点什么"默认文案
  3. 替换了应用 LOGO

---

## 5. PPT 生成

使用 `@oai/artifact-tool` 的 Presentation API 生成了 9 页产品介绍 PPT。

### PPT 内容

| 页码 | 标题 | 内容 |
|------|------|------|
| 1 | 封面 | 备忘录 · 极简桌面笔记应用 |
| 2 | 产品概述 | 即时响应、极简设计、Markdown 三大特性 |
| 3 | 核心功能 | 8 项功能详述 |
| 4 | 产品设计理念 | 零配置、渐进式复杂度、可靠持久化、统一视觉 |
| 5 | 技术架构总览 | Electron 三层架构 |
| 6 | 数据层设计 | MemoStore 代码结构 + 设计要点 |
| 7 | Markdown 编辑体验 | 编辑模式 vs 预览模式 |
| 8 | 测试与质量保障 | 5 文件 59 用例 100% 通过 |
| 9 | 总结与展望 | 技术栈汇总 + 未来方向 |

### 踩坑：PPTX 导出 Bug

**问题**：`presentation.export({ format: "pptx" })` 返回的实际是 PNG 格式，导致 PowerPoint 无法打开。

**根因**：`@oai/artifact-tool` 的 PPTX 导出应使用专用 API：

```js
// ❌ 错误写法（返回 PNG）
const blob = await presentation.export({ format: "pptx" });

// ✅ 正确写法（返回真正的 PPTX）
const pptx = await PresentationFile.exportPptx(presentation);
await pptx.save("output.pptx");
```

---

## 6. 生成产物

| 文件 | 路径 |
|------|------|
| 桌面应用源码 | `memo-app-desktop/` |
| PPT 文件 | `memo-app-desktop/备忘录产品介绍.pptx` |
| PPT 预览图 | `memo-app-desktop/tmp_ppt/renders/slide-1.png` ~ `slide-9.png` |
